{
  "name": "EPUB Image Splitter",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".devcontainer"
  },
  "postCreateCommand": "pip install --upgrade pip && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}